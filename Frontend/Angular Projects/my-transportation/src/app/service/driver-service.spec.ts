import { DriverService } from './driver-service';

describe('DriverService', () => {
  it('should create an instance', () => {
    const directive = new DriverService();
    expect(directive).toBeTruthy();
  });
});
